{
  "name": "markov-bot",
  "version": "1.0.0",
  "description": "A bot that generate random texts on Discord chat.",
  "scripts": {
    "dev": "cross-env NODE_ENV=dev ts-node ./src/index.ts",
    "build": "tsc",
    "start": "node ./dist",
    "docker-run": "npm install && npm run build && npm run start"
  },
  "author": "knownasbot",
  "license": "MIT",
  "dependencies": {
    "axios": "^0.26.1",
    "discord.js": "^14.26.4",
    "dotenv": "^16.0.0",
    "i18next": "^21.8.10",
    "i18next-fs-backend": "^1.1.4"
  },
  "devDependencies": {
    "@types/node": "^18.0.0",
    "cross-env": "^7.0.3",
    "ts-node": "^10.9.2",
    "typescript": "^5.5.4"
  }
}
